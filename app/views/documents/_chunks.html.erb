<% chunk ||= @chunk %>
<% document ||= @document %>
<div class="flex justify-center">
  <div class="flex flex-col w-3/4">
    <% chunks.each do |chunk| %>
      <%= render chunk %>
    <% end %>
  </div>
</div>
