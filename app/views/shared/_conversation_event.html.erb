<%
  event_bg_classes ||= "bg-secondary-200 dark:bg-secondary-800"
  text_class ||= "text-sm"
%>
<%= content_tag :div, id: dom_id, class: "flex justify-start" do %>
  <div class="text-secondary-900 dark:text-secondary-100 px-4 rounded-sm <%= "#{event_bg_classes} #{text_class}" %>">
      <%= event_content %>
  </div>
<% end %>
